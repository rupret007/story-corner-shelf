# R7 exact cable-hook placement proof

This deterministic schematic is the shareable placement/count proof for the R7 cable-hook concept.

- Each shelf level preserves the R6 topology: 6 long-run bays, 3 return-run bays, and 11 support piers.
- Authorized each level: `L2–L7` and `R2–R4` = 9 hook locations.
- Excluded each level: `L1` and `R1` = 2 run-start inside-corner service-clearance locations.
- Two levels therefore show exactly 18 authorized hooks and 4 excluded stations.

The SVG carries matching count metadata and one explicit `hook-location` group per authorized hook. The PNG is a fixed 1800 × 1200 rendering of that SVG. The AI v2 closet image is called out inside the proof as visual intent only; it is not the count authority.

Source topology: the immutable R6 `plan_layout.svg`, whose embedded config SHA-256 is `1bf52b56ab1030f1f88d061a75a8ea609bc82e0081aed105c704b5f6cdd719c8`.

Limitations: station spacing is diagrammatic. This proves intended placement and count, not printed fit or load capacity. R7 CAD and physical qualification remain the authority for geometry, fit, and any future nonzero rating.
